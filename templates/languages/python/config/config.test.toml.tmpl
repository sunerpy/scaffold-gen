# 测试环境配置 (test) —— 安静输出、JSON 结构化、短保留
[log]
level = "warning"           # 测试时只关心告警/错误
format = "json"             # 结构化，便于断言/采集
directory = "logs"
file = ""
log_in_console = false      # 保持测试输出干净
show_line = false
max_size = 10
max_backups = 3
max_age = 3
compress = false
