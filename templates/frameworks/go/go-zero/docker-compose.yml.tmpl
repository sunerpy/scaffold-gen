version: '3.8'

services:
  {{ProjectName}}-api:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile.api
    ports:
      - "{{Port}}:{{Port}}"
    volumes:
      - ../../etc:/root/etc
    restart: unless-stopped

  # Uncomment if you need RPC service
  # {{ProjectName}}-rpc:
  #   build:
  #     context: ../..
  #     dockerfile: deploy/docker/Dockerfile.rpc
  #   ports:
  #     - "{{GrpcPort}}:{{GrpcPort}}"
  #   volumes:
  #     - ../../etc:/root/etc
  #   restart: unless-stopped

  # Database services
  # mysql:
  #   image: mysql:8.0
  #   environment:
  #     MYSQL_ROOT_PASSWORD: rootpassword
  #     MYSQL_DATABASE: {{ProjectName}}
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql

  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data

# volumes:
#   mysql_data:
#   redis_data:
